#include "Structures.h"

void initialiserJoueur(joueur *p)
{
    /*
    p->nom =
    p->prenom =
    p->age =
    p->genre =
    */
}

void PointPartie(partie *point)
{
    /*
    point->point =
    point->temps =
    point->pénalité =
    */
    struct tm instant; // instant.tm_mon && instant.tm_mday && instant.tm_hour && instant.tm_min && instant.tm_sec
} //print("%d%d;%d:%d:%d", ....);    return 0;